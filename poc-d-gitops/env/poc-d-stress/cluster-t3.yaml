# Global values for all applications in the dna-dev environment
global:
  namespace: poc-d-dev  # All applications will be deployed to this namespace

# Application-specific overrides
bf-app-01:
  image:
    tag: "dev-latest"  # This will be the tag for the development version
    
bf-app-02:
  image:
    tag: "dev-latest"  # This will be the tag for the development version
